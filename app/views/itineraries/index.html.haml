= content_for :navbar do
  = render 'shared/navbar'

/ = image_tag avatar_url(current_user)  - Неправильно работает
/ = link_to "Invite a friend", new_user_invitation_path, :class => 'btn btn-primary'

= content_for(:page_header) { 'Listing itineraries' }
= content_for(:page_actions) { link_to 'New itinerary', new_itinerary_path, class: 'btn btn-primary' }

.list-group
  - @itineraries.each do |itinerary|
    = link_to itinerary, class: 'list-group-item' do
      %h4.list-group-item-heading
        %strong= itinerary.name
      %p.list-group-item-text= itinerary.description

= content_for :map do
  = render "shared/map"

= content_for :footer do
  =render 'shared/social'
