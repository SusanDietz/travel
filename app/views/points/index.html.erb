<%= render "shared/sign_reg_buttons" %>
</div>
<h1>Listing points</h1>

<table>
  <tr>
    <th>Lat</th>
    <th>Lng</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @points.each do |point| %>
  <tr>
    <td><%= point.latitude %></td>
    <td><%= point.longitude %></td>
    <td><%= point.name %></td>
    <td><%= point.description %></td>
    <td><%= point.user_id %></td>
    <td><%= link_to 'Show', point %></td>
    <td><%= link_to 'Edit', edit_point_path(point) %></td>
    <td><%= link_to 'Destroy', point, method: :delete, data: { confirm: 'Are you sure?' } %></td>

  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Point', new_point_path %>

<div class="map-wrap">
  <div id="map-canvas" style='width: 800px; height: 400px;'></div>
</div>



<body>
    <header>

    </header>
      <div class="heading">
          <h1>Места</h1>
      </div>
        <div class="heading"></div>
        <h3 class='clearfix'>Ваши места</h3>
        <div class="row">
            <aside class="col-md-7 float-left">
                  <ul class='points-list'>
                  <% @points.each do |point| %>
                    <li>
                    <span class='point-name'> <%= point.name %> </span><br>
                    <%= point.description %>
                    <span class='options'>
                      <%= link_to 'Show', point %>
                      <%= link_to 'Edit', edit_point_path(point) %>
                      <%= link_to 'Destroy', point, method: :delete, data: { confirm: 'Are you sure?' } %>
                    </span>
                    </li>
                <% end %>
                </ul>
            </aside>

  <%= render "shared/map" %>

  <footer>
    <div class='social'>
      <a id='vk' href='http://vk.com/livetravel58' alt='vk'></a>
      <a id= 'fb' href='https://www.facebook.com/groups/livetravel58/' alt='fb'></a>
    </div>
  </footer>
