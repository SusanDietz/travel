<script>
window.gmap_points = <%= raw@hash.to_json %>
</script>
<script>
window.author = <%= current_user.id %>
</script>
            <aside class="col-md-7 float-left">
                  <ul class='points-list'>
                  <% @points.each do |point| %>
                    <li>
                    <span class='point-name'> <%= point.name %> </span><br>
                    <%= point.description %>
                    </li>
                          %p= link_to 'Show', itinerary
                          %p= link_to 'Edit', edit_itinerary_path(itinerary)
                          %p= link_to 'Destroy', itinerary, :method => :delete, :data => { :confirm => 'Are you sure?' }
                <% end %>
                </ul>
            </aside>
